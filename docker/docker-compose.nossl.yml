name: cloud-policy-quantum-access
services:
  web:
    ports:
      - "${DEV_HTTP_PORT:-8080}:8000"
    depends_on:
      - redis_cache
      - vault
      - clamav
  nginx:
    profiles:
      - ssl
