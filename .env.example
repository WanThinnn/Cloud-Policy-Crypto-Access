# Django Configuration
DJANGO_ENV=development
DJANGO_SECRET_KEY=your-secret-key-here-generate-a-strong-one
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (PostgreSQL for production)
# DB_DIR: Path to SQLite database folder on host (e.g. ./data). Leave blank to use container volume.
DB_DIR=
DB_ENGINE=django.db.backends.postgresql
DB_NAME=crypto_access_db
DB_USER=crypto_user
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# CPABE Keys Configuration
# KEYS_DIR: Path to CPABE keys folder on host (e.g. ./keys). Leave blank to use container volume.
KEYS_DIR=

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Redis Configuration (for caching and sessions)
REDIS_URL=redis://127.0.0.1:6379/1

# Admin Configuration
ADMIN_EMAIL=admin@example.com

# Third-party Service Keys (if needed)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
