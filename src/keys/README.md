# Development Keys

> [!WARNING]
> **DO NOT USE THESE KEYS IN PRODUCTION**

The keys stored in this directory (e.g., `vault_token.txt`, `vault_unseal_keys.json`, etc.) are intended for **local development and testing purposes only**. 

- Under no circumstances should these keys be deployed to a production environment.
- Any new keys generated or placed in this directory will be ignored by Git automatically.
